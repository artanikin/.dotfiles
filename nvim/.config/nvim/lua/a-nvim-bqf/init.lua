require('bqf').setup({
    auto_enable = true,
    preview = {
        win_height = 15,
        win_vheight = 15,
        delay_syntax = 50,
        border_chars = {'│', '│', '─', '─', '╭', '╮', '╰', '╯', '█'}
    },
})
