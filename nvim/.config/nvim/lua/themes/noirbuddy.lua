require("noirbuddy").setup({
  -- availabel presets: miami-nights, minimal, kiwi, slate, crt-green, crt-amber
  preset = 'minimal',
  styles = {
    italic = true,
    bold = true,
    underline = false,
    undercurl = true,
  }
})
